#include <stdio.h>
/**
 * main - entry point
 *
 * Return: alway returns 0
 *
 */
int main(void)
{
	int x;

	for (x = 97; x <= 122; x++ )
	{
		putchar(x);
	}

	for (x = 65; x <= 90; x++)
	{
		putchar(x);
	}

	putchar(x);
	return (0)i;
}
